# 拍板卡（决策摘要视图 · 非权威账本）

> 定位：给人 1 分钟读完的**摘要视图**。**唯一权威账本仍是 `DECISION_INDEX.md`**——本卡不得引入新事实，
> 每条只是账本＋原始材料的压缩转述；拍板生效以账本登记为准（铁律 3 单一权威源）。
> 用法：直接回一串即可，例 `1d 3采 4a 7a 8a 9b 11b 12c 14a`（＝下列各项所选字母）。

---

### D-1 · morph 流体终裁 —— **钥匙项，卡住整个 G2**（09-05 官方全文重估，选项扩容）
- **更新后的一句话事实**：Apple 官方 `glassEffectTransition` 文档示例（逐字入册 BENCHMARK §18.2）用的是「**常驻面 ＋ 条件增删面（带 `.matchedGeometry`），两面不同 ID**」；我方现状是「唯一面随选中跨 tile 迁移＋同 ID」——**不是同一形态**。且容器 spacing≈179pt 已是相邻净距 6pt 的 30 倍，按官方「越大越早 blend」**推**得提前量大概率不是瓶颈、瓶颈在**配对两端未被同时认得**（推导非实测；观感仍需你目检）。
- 选项（**旧字母含义不变，向后兼容**）：**(a)** 目检现状 1 分钟（只能复现已知"淡变"，无决策增益）／**(b)** 认可 A 层结构证据（只核销"API 用法合规"，不含观感）／**(d) 新·推荐＝Ⅰ 案**：照 Apple 示例原型加**一个常驻底面**（改动最小）→ 你目检 1 次；**(e)＝Ⅱ 案**：选中面提为**单一常驻面**、位置随选中位移（identity 恒存，官方"geometry 驱动"表述）；**(c)＝Ⅲ 案**：6 面常驻＋tint 区分（＝原推荐，改动最大）。细节表见 BENCHMARK §18.4。
- 执行次序建议：**d → 不行再 e → 仍不行才 c**（一次只动一处，每步四门禁＋入册；每步观感各需你目检 1 次，材质层无静默像素通道）。
- 连带：**只有选 (c) 必须同时定 D-11**（选中区分靠 tint）；选 (e) 不需要；选 (d) 视底面是否抢视觉再定。
- 账本 D-1｜证据 BENCHMARK **§18.2 官方原文与示例全码**／§18.3 数值对算／§18.4 三架构案／§17.3／§3-A1／§0 结论 1b；QUALITY 09-05 四则。
- 附带告知（不占你决策）：`GlassMorphTabBar.swift` L60/L88 注释把「最近边 ≤ 容器 spacing」写成官方判据，官方无此表述，已登记随 G2 首轮 `.swift` 批次改为「我方启发式模型」（§18.5）。

### D-3 + D-7 / D-8 / D-9 · 设置页（一次裁决四格）
- 一句话事实：现在**两套结构两个入口**（8 子页 vs 5 摘要卡），5 卡里 **4 张是纯只读文本、体内没有任何跳转实现** → 用户到此即死路（`SettingsCompletePage.swift` L74/83/9x 证据在册）。
- **D-3**：采纳单一结构（左 6 栏 sidebar＋右 pane，取消双轨）／不采纳（维持双轨）。建议＝采纳（分 4 步小提交，材质改造一律放最后，5 条已核销不变量不得回退）。
- **D-7** 容器形态：(a) 维持 sheet（现状，Esc＋xmark 已核销）／(b) 独立 Settings 窗口。建议 (a)。
- **D-8** 概览页：(a) 保留＋每行"编辑…"跳转／(b) 删概览只留编辑 pane。建议 (a)（它就是修死路的那一步）。
- **D-9** 记忆/工作区路径：(a) 升为可编辑／(b) 明确标注只读＋文案说明。两者都消除"同类信息两处不同能力"的误解，(a) 更彻底但要加写入校验。
- 账本 D-3/7/8/9｜材料 `docs/SETTINGS_IA_PROPOSAL.md` §二缺陷表→§三映射表→§五切分。

### D-4 · 主题 manifest 假参数
- 一句话事实：manifest 里写了我们不支持的字段时**静默无效**，用户以为生效了。
- (a) 显式拒绝＋提示／(b) 维持现状。建议 (a)——与"玻璃诚实"同一条口径线。

### D-11 · tint 作用域
- (a) 全局装饰色／(b) 仅功能件（官方克制口径）／(c) 由主题插件自己声明。
- 建议 (b)；注意 **D-1 选 (c) 时选中区分靠 tint**，(b) 不冲突（选中 tile 属功能件）。

### D-12 · F5＋F6 批次
- F6＝折叠 rail 左上角与红绿灯/展开按钮**静态三方交叠**（A17 实测命中）；F5＝sheet 自铺底。
- (a) 都做／(b) 都延后／(c) 部分（建议：F6 做——它是可证的交叠缺陷；F5 等 G3 目检再定）。

### D-13 · leaks 门禁口径 —— **09-05 18:09 实测否证，自动闭环（你无需出场）**
- 结论：第四门禁已 @当前HEAD 实跑 `rc=0`（`0 leaks for 0 total leaked bytes`／整段 28s／marker 在册）⇒ 原三案（重启会话／降级口径／挂起）全部作废，**拍板池 9→8 项**。
- 撤回：原「三条通道 wedge」归因未经有效对照即登记（当时金丝雀对照因被测侧命令参数写错而从未成立），本轮对照实测三通道均可用；挂起曾真实发生但**病因未定、不可复现**（P1 补记⓬）。

### D-14 · launchd 重门禁静默化（本轮新发现）
- 一句话事实：`com.harness.ci11.pr` **已加载**且 `RunAtLoad` ⇒ **每次登录都在你必然到场时跑全量门禁**；全仓无任何 idle 守卫。
- 守卫已落地 `tools/ci-quiet.sh`（四路径实测 rc 0/7/3/5，中止后无进程残留）。
- (a) 改 plist 让它调守卫（需 `launchctl` 重载＝动你的守护进程，**我不会擅自做**）／(b) 维持现状／(c) 卸载它、改由我在缺席窗口手跑。建议 (a)。

### A14 · 减弱透明度语义
- (a) 保 solid 纯色（现状，已验收）／(b) 增加 frosted 中间态。(a) 与现有降级不变量最诚实。

### D-2 · 悬停反馈（零动作）
- 注释里的"材质自带"宣称已撤销＋`.interactive()` 已按官方补齐并落测试；**残余只有你目检一次悬停观感**，随 G3 走查顺带完成。

---

**还需要你出场的另外三件（不是选择题）**：① Codex 截图（W1–W8 观察窗，粘图即可，我落盘 `~/harness-wt/evidence/codex/`）；② G3 走查（你操作，我只读 `axdump`/`wl`/`screencapture -l` 同帧取证）——**走查前请注意 P6：当前在跑的进程是 9-3 的 debug 二进制，需用当前 HEAD 重启**；③ G4 兼容双口径已入册（DoD 达成≠拿来即用全量成立，层2 5 样本 3 通 2 阻塞；**09-05 新增层3 主题类普查 2 样本＝结构性不适用**——上游 `dsh.client.platform` 全仓 39/39 只有 `"web"`，社区主题插件是浏览器 DOM/CSS 层，原生客户端机制上不可能，详见 CENSUS 层3）。


---

## 你的目标文本待更新清单（v8→v9，Agent 不改你持有的文本，集中上报一次改完）

| # | v8 原文（节选） | 实况（证据） | 建议改法 |
|---|---|---|---|
| 1 | 「D-1 …修复验证用 **ImageRenderer 像素帧**判定流体融合」 | 该通道 09-02/09-03 实测**不渲染玻璃**（BENCHMARK §0 首行） | 「材质层验证＝结构断言（参与 morph 玻璃面数==`segments.count`）＋你目检 1 次或认可结构证据」 |
| 2 | 「每轮：四门禁 + **before/after 离屏渲染取证**」 | 内容层可用、玻璃层不可用（§0 结论 1b，正对照在册） | 「每轮先定层：内容层轮交 A 层离屏像素对；材质层轮交结构断言＋目检」 |
| 3 | 「G0：R1 余项核销…**#3/#6/#7/#8 依 A 层证据＋你的二选一裁决**」 | **已于 09-04 全部 ☑ 核销**（池 B 拍板；R1 收口表 1–9 行全 ✅） | 从待办移除，改记「G0 R1 全 ✅（09-04 池 B）」 |
| 4 | 头部「覆盖率 **97.57%**，四门禁全绿」 | 覆盖率当前权威 **97.56%**（09-05 11:45 main rc=0：9,755/238）；四门禁**此刻为 4/4 真**——09-05 18:09 `leaks` rc=0 marker 补齐（此前 3/4，见 D-13 闭环） | 数字改 97.56%；「四门禁全绿」**可以写**，加注「marker @09-05 18:09，G3 走查须 @最终HEAD 复验」 |
