# 决策总账（D-INDEX）——DoD 首条的单一检查点

> 目的：v8 DoD「D-1~D-6 拍板记录齐（+后续新增项）」的**唯一权威账本**（铁律 3 单一文档源）。
> 每项拍板后由 Agent 记录「决定 + 日期 + 落点 HEAD」；G3 手册 §4 为速览副本，账本以本文件为准。

| ID | 议题 | 选项 | 状态 | 证据/来源锚 |
|----|------|------|------|-------------|
| **D-1** | morph 流体判定终裁（头号）**·09-05 决策重述** | (a) 目检 G3 A-a 1 分钟 ／ (b) 认可 A 层结构证据核销 ／ **(c) 新·推荐**：先做 §3-A1 根因改造（未选中 tile 亦常驻玻璃面，选中改 tint/前景区分），静默判据＝单测断言「native 态参与 morph 玻璃面数 == segments.count」，改造后你再 1 分钟目检「液体颈」；仍无融合则按 §1.14 终裁重估 `.matchedGeometry` 去留，morph 不可得即回落 F2/tint 强调并改判 P1 口径 | ☐ | **证据边界（09-05 实测钉住，勿再按旧口径读）**：① 本仓 A 层**无任何可捕获玻璃像素的通道**（§0 通道表首行即 `ImageRenderer` 离屏位图=**完全不渲染玻璃**，09-03 实测；另 `cacheDisplay` 0/96000、`displayIgnoringOpacity` 0/613、`CALayer.render` 0/611、`dataWithPDF` 不捕获内容、边界外窗+screencapture 挂起）⇒ v8 原文「用 ImageRenderer 像素帧判定流体融合」不可执行，**该判据 09-02 已由 QUALITY 纠偏过一次**（本轮 (c) 案是补执行路径，非首次发现）；② 8 项结构判据只能证明**API 用法合官方语义**，推不出**视觉上流体融合**，故 (b) 的有效边界=核销「实现合规」，不含「观感达标」；③ 源码实测容器内运行时**仅 1 个玻璃面**（`GlassMorphTabBar.swift` L225–239：`TileFaceMode.resolve` 非选中走 `.plain`，`.glassEffect` 全仓仅 L231 一处）⇒ §3-A1=❌根因 GAP 未修，此时目检 (a) 只能复现「淡变」已知结论，**不产生决策增益**。故选 (a)/(b) 请连同①②③的边界一并确认；选 (c) 则需同时裁 D-11（tint 作用域＝A1 选中区分的实现手段来源）与 A12 面数/性能护栏复测 \| **⁽⁰⁹⁻⁰⁵ᴮ⁾ 官方全文重述（BENCHMARK §18，本机 SDK `.swiftdoc` 新通道）**：官方 `glassEffectTransition(_:)` 示例（逐字入册 §18.2）显示 morph 的官方原型形态是「**常驻面＋条件增删面（带 `.matchedGeometry`）、两面不同 ID**」，与我方现状「唯一面随选中跨 tile 迁移＋同 ID」不同形态；我方容器 spacing≈179pt 已是相邻净距 6pt 的 30 倍（§18.3 静态对算），按官方「越大越早 blend」推得**spacing 提前量大概率非瓶颈、瓶颈在配对两端未被同时认得**（标注为推导，非像素实测），故原「先改 A2 spacing」的便宜路径**作废**。⇒ D-1 菜单扩为三架构案（§18.4）：**Ⅰ 常驻底面＋条件面（＝Apple 示例原型，改动最小）**／Ⅱ 单一常驻面随选中位移／Ⅲ 六面常驻（＝原 (c) 案）。**Agent 推荐次序改为一行话：先 Ⅰ、不行再 Ⅱ、仍不行才 Ⅲ**（每步一次目检不可避免）。**答案字母映射（向后兼容，旧字母不变）：Ⅰ=新增 (d)／Ⅱ=新增 (e)／Ⅲ=原 (c)** ⇒ 用户回 `1d` 即选 Ⅰ 案。另纠出一处过度归属（代码注释把「最近边 ≤ 容器 spacing」称作官方条件，官方无此表述，登记 §18.5，随 G2 首轮 `.swift` 批次合并修）**⁽⁰⁹⁻⁰⁵ᵉ⁾ 该子句作废：官方在线教程文章实测有此文（逐字＋curl 复现在 §18.5），注释维持「官方」名义为正确；残余仅 L60 后半句「取整即覆盖全部切换距离」需标「我方保守取整」** |
| **D-2** | `interactive` 宣称口径 | 已半结：注释「材质自带」宣称已撤销（BENCHMARK §9）→ 残余=悬停反馈目检（归 G3 A-h） | ◐ | BENCHMARK §6/§9 |
| **D-3** | 设置页 C4 卡片归组 | 与 SETTINGS_IA_PROPOSAL **合并裁决**（IA_PROPOSAL L77：不必单列） | ☐ | docs/SETTINGS_IA_PROPOSAL.md |
| **D-4** | 主题 manifest 假参数（A6） | 显式拒绝不支持字段+提示 / 维持现状 | ☐ | BENCHMARK §3-A6/§6。**⚠️ 09-05 事实纠偏（会改变取舍权重）**：**「维持现状」≠ 静默无效**——`17e9f22`（08-29 P2.3）已把两字段做成「接收＋校验＋UI 明示不生效＋MCP 非法回落」的诚实态（A6 行有代码与测试锚点）。⇒ 本项真正的问法是「已诚实声明不生效的预留字段，是否还要进一步**直接拒绝**（主题作者会拿到报错而非一段说明文字）」，属体验取向选择，不是安全缺口修复 |
| **D-7** | 设置容器形态（**09-05 补登**：此前只存在于 IA_PROPOSAL §七，账本漏登） | (a) 维持 sheet（现状，Esc/xmark 闭环已核销）/ (b) 独立 `Settings` 窗口 | ☐ | docs/SETTINGS_IA_PROPOSAL.md §七（提案内建议=a）；与 D-3 合并裁决 |
| **D-8** | 设置概览页去留（**09-05 补登**，同上） | 概览页保留 + 每行「编辑…」跳转（提案默认）/ 删概览页只留 6 编辑 pane | ☐ | docs/SETTINGS_IA_PROPOSAL.md §七 |
| **D-9** | 记忆/工作区可编辑性（**09-05 补登**，同上） | 升为可编辑 / 明确标为只读并在文案说明（IA-4 二选一） | ☐ | docs/SETTINGS_IA_PROPOSAL.md §七 |
| **D-5** | G4 层2（Cordis sidecar 跑社区插件） | 做 / 不做 | ✅ 09-04 = 做（G4c 启动） | CENSUS **四形态总表**；实施设计 docs/G4C_SIDECAR_DESIGN.md |
| **D-6** | 24 零事件会话处置 | 删（DB 写需明示+二次确认）/ 留 | ✅ 09-04 拍板 = 删；✅ **09-05 已执行**（26→2，三重校验通过） | QUALITY DB 只读复核（26=24+2）；Keychain 旧凭证为可选项 |
| **D-10** | 玻璃折射源修法（材质走向，**最重要**） | (a) 窗口透明底 / (b) backgroundExtensionEffect / (c) 接受扁平 | ✅ 09-04 = (a) | BENCHMARK §13-F4/§15-A15 根因链 |
| **D-11** | 主题 tint 作用域（A13） | 全局装饰 / 仅功能件（官方口径）/ 主题可声明 | ☐ | BENCHMARK §12/§14 |
| **D-12** | F5（撤 sheet 自铺底）+F6（rail 避让带）批次 | 做 / 延后 / 部分（修后各需 1 分钟目检或认可静态证据） | ☐ | BENCHMARK §14/§15；F6(b) overlay 落带官方沉默、终裁归 G3 A-b 目检（㊷） |
| **D-13** | **~~leaks 门禁在本机不可用~~ → 09-05 18:09 实测否证，自动闭环（不需你出场）** | 原三案全部作废（(a) 重启登录会话／(b) RSS 护栏降级口径／(c) 挂起待恢复）——第四门禁已 @当前HEAD 实跑 `rc=0`：`leaks --atExit -- .build/debug/MemProbe 500` = `0 leaks for 0 total leaked bytes`，marker `2026-09-05 18:09:07 mode=leaks rc=0`，整段 28s | ✅ 09-05 闭环（**前提不成立而自动关闭，非用户拍板**） | **否证对照（本轮实测，全 A 层零前台）**：`sample <sleep 金丝雀> 1 -file` rc=0/117 行／`sample 58301 1 -file` rc=0/1219 行（GUI 进程 attach 正常）／`leaks <金丝雀 pid>` rc=0／`leaks --atExit -- MemProbe 5` rc=0/1s ⇒ 三条依赖 task 端口的通道均可用。**诚实边界**：当时挂起有在册证据（MemProbe 停 T 态、ps 状态 `TN`），但原始日志未留存 ⇒ 真实失败模式不可反推，故改判「间歇性故障、病因未定、已不可复现」；「登录会话/调试通道退化」属未经对照支持的归因，**撤回**；门禁脚本内写死该归因的失败提示同步改为中性处置顺序（本轮提交） |
| **D-14** | launchd 重门禁静默化（新发现） | (a) `com.harness.ci11.pr` 的 `ProgramArguments` 改调 `tools/ci-quiet.sh pr`（登录不再立即抢占，等缺席窗口才跑，你回场即整组回收）／(b) 维持现状（登录即跑全量 pr）／(c) 卸载该 LaunchAgent，改由 Agent 在缺席窗口手跑 | ☐ 待批 | **实测现状**：`launchctl list` 显示该 job **已加载**，plist 为 `RunAtLoad=true`+`KeepAlive=false` ⇒ **每次登录/加载即在你必然到场时跑全量 pr 门禁**（Lint+Format+编译+788 测试）；全仓 `grep HIDIdleTime` 零命中 ⇒ 「重门禁只在缺席窗口跑」此前**纯靠人工判断、无任何守卫**。守卫工具已入仓（本轮），四路径实测 rc 正确。⚠️ 改 plist 需 `launchctl` 重载 = 动用户级守护进程，**必须你批准**，Agent 不擅自动手（与 D-13「绝不代用户动会话/守护进程」同源）。|
| **D-16** | MCP 卸载后工具从列表消失的延迟（**09-06 新立**，全量门禁 flake 追出的产品侧疑点） | (a) **推荐**：`removeMCPServer` 即时从工具列表移除＋服务器断开转后台（用户语义＝「卸载即消失」）／(b) 承认非即时承诺：改 UI 文案＋测试上限对齐导入侧 20s／(c) 视为负载现象不处理（仅低负载门禁下验收） | ✅ | 实测两失败：5.098s 撞 5s 上限、上限对齐 20s 后**同一断言** 20.215s 再失败（`/tmp/ci_main_aft1.log`）；低负载下该用例绿（@0934c5a 四门真跑）；附带项＝flakes 定量（`--parallel` 连跑 N 次记失败率）；(c) 与「宁可多报不可假绿」冲突，Agent 不推荐 |
| **D-17** | 终端命令把调用方的 Swift 协作线程占死到命令结束（**09-06 新立**，与 D-16 同族放大器） | (a) **推荐**：`withCheckedThrowingContinuation` 的**阻塞体**整体改投独立线程（`Thread.detachNewThread`）〔⚠️ 本行原写「本文件 L413/L441 已是同型用法」**已被 grep 否证**：全仓 `detachNewThread` 当时零命中，既有读者管道用的是 `DispatchQueue.global`（内核线程，本就安全）⇒ 该引用属未核实即入册，见 QUALITY 09-06 补记⑪〕——轮询/超时/取消/`group.wait` 语义一行不改，仅把「占死协作线程」摘掉；／(b) 改官方 `terminationHandler` + `readabilityHandler` 全异步化（改动面大，含捕获上限与取消语义）；／(c) 维持现状（接受高并发终端工具时的调度饥饿） | ✅ 09-06 闭环（采 (a)，含判据自我否证一次） | **代码事实**：`Packages/Terminal/Sources/Terminal.swift` L123 起，闭包体含 `pollUntilExit`（50ms `Thread.sleep` 轮询 L221）＋ `waitUntilExit()`（L151）＋ `group.wait()`，全部同步执行在调用方执行器＝协作线程池上，**每条终端命令占死一条协作线程直到命令结束**（本仓 `exec_command` 是 Agent 高频工具 ⇒ 并发即饥饿）。官方对 `waitUntilExit()` 的原文＝「Blocks the process until the receiver is finished.」＋「…Then it polls the current run loop using NSDefaultRunLoopMode until the task completes.」（协作线程池线程没有运行循环）。同族已核：MCP 侧的 `Task.detached { process.waitUntilExit() }` **本轮已按官方 `terminationHandler` 改掉**（见 P1/QUALITY 09-06）；`XPCPluginHost.ProcessCommandRunner.run` 是同步协议方法内的短命令（launchctl），影响可忽略，仅记录。⚠️ 与既有「协作池调度停滞」环境口径**分开归因**：那条观察到的是「线程空闲但任务不派发」，本轮两处属「线程被我们主动阻塞」，两者不同病，勿混用结论 |
| **D-18** | MCP 工具名 `mcp_<server>_<tool>` 的前缀删除跨服务器误删面（**09-06 新立**，实测已复现） | (a) **推荐**：`disconnect` 按**该客户端工具清单的精确名**逐个注销（`client.listTools()` → `mcp_\(name)_\(tool)`），仅清单不可得（进程已死／握手失败）时回落前缀并写 `toolsLoadWarning` 告警；／(b) 工具名生成规则改为可逆转义（服务器名内 `_` 转义）——彻底但**破坏 wire 展示名**（工具页 id、既有会话 `isExecuted` 记录、主题/技能引用全部受影响），冻结期不宜；／(c) 接受现状（低概率静默误删） | ✅ 09-06 闭环（采 (a)，但归属判据实现得比原案更强） | **实测复现（09-06 临时探针，跑完即删）**：注册 `my`（工具 `server_x`）与 `my_server`（工具 `y`）两个内存客户端 ⇒ `before=["mcp_my_server_x","mcp_my_server_y"]`，`disconnect(name:"my")` 后 `after=[]`——其中 `mcp_my_server_y` 属 `my_server`，被**静默连带删除**。触发条件＝一名是另一名的下划线前缀＋工具名补齐该前缀；社区常见命名多用 `-` ⇒ 概率低但后果是「A 的卸载把 B 的工具抹了」。代码事实＝`Packages/MCP/Sources/MCP.swift` disconnect 的 `hasPrefix("mcp_\(name)_")` 循环（本轮已把它移到「先摘注册表」位置，但匹配规则未动，本项与 D-16 闭环相互独立） |
| **D-19** | 聊天顶栏材质游离在玻璃体系与主题驱动之外（**09-06 新立**，G1a 轴1 审计产出） | (a) 顶栏改 `.glassSurface(...)` 纳入体系（受主题 tint/材质驱动 + 进减弱透明度降级链；需定档位，且顶栏横跨整窗宽度，形态变化必须目检）／(b) 保留 Material 但材质改由主题给（沿用既有 `VisualEffectMaterial` 通路，仍不产 Liquid Glass 折射）／(c) 维持现状并显式声明「顶栏非玻璃面」（写进 BENCHMARK 口径，别再当已覆盖） | ☐ 待裁 | **代码事实（实测，非推测）**：`ChatAreaView.swift:14` `ChatTopBar(...).background(.ultraThinMaterial)` 是全仓仅有的 2 处 SwiftUI Material 直接用法之一（另一处 `ContentView.swift:80`＝Toast 胶囊临时浮层，非结构面）。玻璃体系入口是 `.glassSurface(...)`，其材质/tint 解析链在 `GlassSurface.swift:45`（主题注入）与 `:140`（「显式 tint > 主题 glassTintHex > 无 tint」）——**顶栏不走这条路，所以 MCP 主题插件管不到它**；同理 `GlassSurfaceModifier.currentMode` 的 native/solid/solid 减弱透明度降级链也不覆盖顶栏。⚠️ 属观感变更且与 **D-11（tint 作用域）** 同源，Agent 不自动改；Toast 那处可单独豁免（生命周期短、面积小） |
| **D-20** | 启动期 MCP 连接 × 用户卸载竞态 ⇒「已卸载服务器复活」（**09-06 新立并当场闭环**，＝观察项 F-a 的真实根因，不是测试抖动） | (a) **采此**：`registerStartupTools()` 循环内加两道 TOCTOU 守卫（连接前重查 servers.json ⇒ 已卸载即 `continue`；连接完成后再查 ⇒ 已卸载即 `disconnect` 回收），并新增测试缝 `startupMCPConnectTestGate` 使该窗口可被**结构性**复现（零墙钟）；／(b) 卸载时取消在途启动 `Task` 并等其结束（需取消令牌＋与 `refreshMCPServers` 的次序约定，且关不掉「连接已完成之后才看到卸载」这半个窗口）；／(c) 启动期连接改同步阻塞（窗口归零但拖慢启动，违背本文件既有「后台进行，不阻塞启动」口径） | ✅ 09-06 闭环（采 (a)；自动闭环理由同 D-17/D-18：纯技术缺陷、修法无产品语义分歧、与你 09-06「执行」指令一致；推翻成本一条 revert） | **代码事实**：`Apps/HarnessApp/Sources/ViewModels/AppViewModel.swift` `registerStartupTools()` 先 `MCPDiscovery.loadConfigs(url: mcpConfigURL)` 取**快照**，再在后台 `Task` 内逐个 `await mcpManager.connectStdio(config, into: toolRegistry)`；`connectStdio` **成功与 catch 分支都会登记 descriptor**（`isAvailable:false` 亦然）⇒ 卸载若落在「快照之后、该次连接完成之前」，`removeMCPServer` 的 disconnect 先失效、服务器随后复活（症状＝主题不回落系统基准／工具重新注册／列表条目复活）。并行负载下 await 让出点增多 ⇒ 窗口变宽，这正是 F-a 只在 `--parallel` 稀发复现的机制。**判别器（确定性，零墙钟/零 sleep）**：`Apps/HarnessApp/Tests/MCPRemoveDuringStartupRaceTests.swift` 用双向 actor 信号门把交错钉成固定次序（循环停在「即将连接 victim」→ 窗口内按 UI 同一路径 `removeMCPServer` → 放行连接 → 越过 **sentinel** 即证守卫链跑完）；夹具取 `/usr/bin/false`（走 catch 分支但同样登记 ⇒ 判别面存在），无真子进程依赖、无网络、无窗口（静默 A 层合规）。**变异实测**：删掉两道守卫 ⇒ 用例转红且签名精确 `descriptor 仍在册：["local", "race-victim"]` ⇒ 竞态真实、断言非空。修复后 passed 0.021s；全量 `Test run with 798 tests`；**并行态 6/6 轮复跑全绿**（`/tmp/fix_par_1..6.log`，F-a/F-b 均未再出现）。**口径修正**：F-a 由「观察项」改记「根因闭环（6/6 并行复跑未见＝强证据，**非穷尽证明**）」；F-b 仍开放，下一步判据不变（打印失败时的 `NSError` 域/码）。 |
  · **⚠️ 09-06 前提否证（见下方闭环条）**：两条失败均与「卸载延迟」无关——真因一是测试按下标取卸载对象（内置演示服务器 `local` 按名排序在前且注册时机不确定），真因二是 `AppViewModel.refreshTools` 跨 actor-hop 读-改-写**丢失更新**（旧快照后写＝已注销工具被写回，等多久都不消失）。(b)/(c) 两案的前提随之消失。 |
| **D-15** | **A2 玻璃容器 spacing 取值（09-05 改判：与 D-1 解耦的独立合规项）** | (a) 保 179 现状（远距切换全覆盖 morph，代价＝静止态按官方口径**必然**过度融合）／**(b) 新·推荐**：收敛到 `adjacentSpacing()`=52 ＋ 远距切换显式 `.materialize`（官方给出的正是这种配对表达）／(c) 与 D-1 绑定，先裁 Ⅰ 案再定 spacing | ☐ | BENCHMARK §19（三句此前全仓缺录的官方逐字原文＋实数对算 179/52/6＝29.8×与 8.7×）；关键原文＝「A spacing value on the container that’s larger than the spacing of an interior HStack, VStack, or other layout container causes Liquid Glass effects to **blend together at rest**」；⚠️ 官方未给安全倍数阈值 ⇒ 收益可定性、观感仍需目检（§0 无玻璃像素通道） |
| **A14** | 减弱透明度语义（v8 手册补登记）**·09-05 证据加固** | 保 solid 纯色（现状已验收）/ 增 frosted 中间态 | ☐ | BENCHMARK §12-A14 ＋ **§23**（双原文并列：WWDC219「frostier」观感描述 vs `accessibilityReduceTransparency` API 页「should be opaque」**对开发者的指令** ⇒ 保 solid 有明文支持）｜⚠️ 本行状态 ☐ 曾被 `decision-pending.sh` 旧口径漏计（只数 `D-` 前缀），现合计 13 项 |
| **轴2** | Codex 界面取证方式 | (a) 你丢截图 / (b) 择时只读截观察窗 / (c) 延后 | ☐ | BENCHMARK §16 工作单；UI_CODEX_ALIGNMENT 重判节 |
| **RSS 可见态组** | 可见态性能基线 | 你开 App 自然用 ≥10 分钟（我后台只读）/ 免 | ☐ | PERFORMANCE 正式基线节 |
| **G1-SCOPE** | 双轴审计范围追认（DoD「范围经确认」条的正式落点） | 认可现行范围（轴1 A1–A19+材质提案 F3/F4 系列；轴2 W1–W8 观察窗清单）/ 或指出增删 | ✅ 09-04 追认 | BENCHMARK §2/§10/§12/§15/§16 全链在册；本轮补登记（发现 DoD 该条此前无正式确认载体） |

## 拍板记录区（拍一条记一条）

- **D-6** ✅ = 删（24 零事件会话）2026-09-04：用户 chat 答复「1」，双位落位记录（列项首位 = D-6 × 选项首位 =「删」；如属误读一条 revert 即回退）。二次确认口径执行中：DB 只读复核 26=24+2 吻合在册 QUALITY 口径；24 清单 = 18 历史（08-20 16:32 ～ 09-02 22:42）+ 6 本轮副产（BB0112F7/8FF4EE09/618BB207/DF74B23A/6F012001/9191AD8D，09-03 12:02~12:29 登记在案），全部零事件、无标题、turn=0；备份就位（`/tmp/d6-backup/`：`sessions.sqlite.pre-D6` 整库快照 + `sessions_d6_rows.sql` 24 条行级 INSERT，另于执行前追加 `sessions.sqlite.pre-exec` 第三备份）。✅ **09-05 已执行**：用户 chat 答复「执行」= 末道二次确认消费，DELETE 落库；后置三重校验全过（sessions 26→2 / 剩余会话零事件数=0 / 孤儿事件=0），2 条有事件会话与 events/projects 表未动。Keychain 旧凭证 = 可选项，用户手动清理口径不变。

- **D-5** ✅ = 做（G4c 启动）2026-09-04：用户 chat 答复「1」，双位落位记录（blocked#6 报告后列项首位 = D-5 × 选项首位 =「做」；如属误读一条 revert 即回退，代码面尚未开跑、回退成本最低）。Agent 材料立场如实留档：层2 增量收益 ≈0（CENSUS 总表：形态①②层1 已成立、③④不适用），但用户历史立场（社区插件拿来即用）明确，**做=用户裁决非材料推导**。执行口径 = docs/G4C_SIDECAR_DESIGN.md（S-1~S-4 默认策略可否决；C0–C4 全 A 层验收；安全红线含「JS=任意代码执行」授权声明）。

- **G1-SCOPE** ✅ 2026-09-04：用户追认现行双轴审计范围（轴1 A1–A19 + 材质提案 F3/F4 系列；轴2 W1–W8 观察窗清单），无增删。拍板载体：chat 答复「1」（对应 Agent 列项「认可池 B + G1-SCOPE」）。DoD「范围经确认」条闭合。
- **DoD「G1 双轴 checklist 入册且范围经确认」格 · 双口径核销（09-05）**：**字面 ✅**（轴1 checklist=BENCHMARK §2 A1–A19 全册；轴2 checklist=§16 W1–W8 观察窗清单；范围=G1-SCOPE 09-04 追认）；**实质 ⏳**（W1–W8 的「Codex 侧观察」列全空，缺你截图 ⇒ `UI_CODEX_ALIGNMENT` 无法刷新，该项挂在 DoD 的 G3 格上）。⇒ 申报口径：**G1 格勾，但不得据此宣称「Codex 对标已完成」**——与 G4「DoD 达成≠诉求达成」同一处理规则。
- **池 B（R1 #3/#6a/#6b/#7/#8 真机视觉帧补证，㉔ 二选一）** ✅ 2026-09-04：用户选第二项=「认可 A 层进程内证据即满足该子句」→ 静默核销（证据：#3 源码共享+溢出 10 项实测；#6a ThemeLiveRenderTests；#6b FileThemePackageTests；#7 SidebarDropHighlightRenderTests+noChange 单测；#8 GlassSurfaceTests 降级链+测试缝——均在册含 02:26 xcresult 867 项）。#7「用户手动拖一次」子句按 ㉓ 第二分支（认可在册）闭环，A-f 转 G3 可选观察项。**R1 八项全 ✅**。落点 HEAD：本提交（tag `g0-baseline-20260904`）。

- **D-10** ✅ = (a) 窗口透明底 2026-09-04：用户 chat 答复「1」，双位落位记录（G0 收口报告「下一步」第 1 项 = D-10 × 其选项首位 = (a) 透明窗底案；如属误读一条 revert 即回退）。执行=@`ee1da43` 三处联动（makeWindow 透明装配测试缝 / 实底只贴主区 / 侧栏 GlassSurface(.regular) 底）+ WindowGlassSamplingTests 结构测试。**透窗采样实况终裁 = G3 A-d 目检**（㊹ 风险注记：官方无透窗采样明文，锚定 legacy behindWindow 在册能力）；F5（sheet 自铺底）归 D-12 未拍不动，已知其同窗下仍挡 sheet 区折射。

- **账本完整性补登 09-05**：全量 ID 扫描（`docs/*.md` + P1/QUALITY 交叉比对）发现 **D-7/D-8/D-9 三项被 BENCHMARK/SETTINGS_IA_PROPOSAL 引用却从未入本账本**——后果是 G3 手册 §0 P5「§4 全部 D 项有拍板记录」按副本走查会整组漏掉设置页决策簇（D-3 + D-7/8/9）。已补齐三行并同步 G3 §4 副本；**口径重申**：任何文档新设 D-x/A-x 编号，必须同批写入本账本，否则视为未登记。
- **D-13 背景（09-05）**：门禁清偿轮实测 `leaks --atExit` 使 MemProbe 停在 **T 态**后自身死锁（`ps` 状态 `TN`）；`leaks <pid>` 亦 90s 无返回；同一时间窗内 `lldb -b -o run` 与 `sample <pid>` 亦无输出——三条依赖 task 端口的工具同时失效，而 MemProbe 直跑 2s/rc=0，故**不是被测代码问题**。09-04 21:20 该门曾 rc=0 ⇒ 属本机登录会话/调试通道状态退化（首要嫌疑：本人以 SIGKILL 打断 lldb attach；未证实）。**门禁脚本已改为有界+显式失败（rc=142 并清理 T 态孤儿）**，绝不静默放行；RSS 峰值护栏仅作补充证据、不替代 leaks 判据（口径不可自审自批，故立 D-13 请你拍板）。 **【09-05 18:09 否证与撤回】**上列通道级归因不成立：本轮以「同命令 × 两类目标」对照实测，`sample` 对无关进程与被测 GUI 进程均 rc=0，`leaks` 对金丝雀 attach 与 `--atExit` 双路径均 rc=0，且 `tools/ci-local.sh leaks` @当前HEAD 整段 rc=0 ⇒ 第四门禁已真实通过，无需任何环境恢复动作。教训：归因前必须做对照并自证命令语法正确（`sample` 参数误用会瞬时 rc=255 打印 usage，易被记成「无输出」）。

- **D-16 立项依据 09-06**：为验证 §21.2 批次落地而连跑门禁期间，捕获三类不同症状（详见 QUALITY 09-06 补记③）。其中 (a) 等待判据写错已在 @0934c5a 修复（测试侧，逻辑判据纠正）；(b) MCP 卸载延迟**未改代码**——我先做的「上限 5s→20s 与导入侧对齐」试探在实测后**主动撤销**，因为放宽上限只会把「卸载后工具迟迟不消失」这一用户可感知问题藏进更长的等待里；(c) `SessionDB` 打开失败属负载型，登记不修。Agent 立场：推荐 (a)，因它同时消除 flake 与语义歧义，改动面局限在卸载路径。

- **D-16 ✅ 2026-09-06（前提否证型闭环，依 D-13 先例：非用户拍板＝你少出场一次；推翻成本一条 revert）**：本轮为落地 §21.2 批次而连跑门禁时，把该症状追到**两条与「卸载延迟」无关的真因**并各自修掉＋配了**变异实测判别器**：
  · **真因一（测试侧，非产品缺陷）**：`MCPServerManager.servers()` 按名排序，而 `AppViewModel.registerStartupTools()` 会把内置内存演示服务器 `MockMCPClient(name:"local")` 注册进管理器，其进入 `mcpServers` 的时机取决于启动期后台发现 Task 的进度 ⇒ 凡名字排在 `local` 之后的用例（`rm-test`/`regtest`/`theme-mcp`/`dsh-crew-*`）用 `mcpServers[0]`/`.first` 随时可能**选错卸载/重启对象**，`count == 1`/`isEmpty` 亦随时翻车。⇒ 「卸载了，工具还在」＝卸载错了对象。修法：新增 `Apps/HarnessApp/Tests/MCPUserListFilter.swift`（`userMCPServers` / `userMCPServer(named:)` 为唯一安全口径），三个测试文件（`AppViewModelMCPServerTests` / `AppViewModelThemePluginTests` / `MCPCommunityAppFlowTests`）取项一律按名。
  · **真因二（产品缺陷，已修）**：`AppViewModel.refreshTools()` 是「`await toolRegistry.schemas()` → 写 `tools`」的**跨 actor-hop 读-改-写**，而它至少被 8 处调用（启动期发现 Task、MCP `tools/list_changed`、沙箱根变更、导入/重启/卸载），并发时**先读者后写＝旧快照覆盖新快照** ⇒ 已注销工具被写回列表，且此后无人再刷就**永不消失**——这正是「5.098s 撞上限、把上限对齐 20s 后又在 20.215s 失败」的签名（等待上限与根因无关）。修法：刷新排成串行链（`toolsRefreshTail`），语义保证＝`await refreshTools()` 返回时快照必含本次调用前已发生的注册表变更。
  · **判别器强度（变异实测，非推测）**：`AppViewModelToolsRefreshRaceTests`（600 轮，零子进程，0.4s）——把 `refreshTools` 换回串行化**之前**的原实现 ⇒ **600/600 全红**；串行化后 ⇒ **0/600**。`MCPProcessDeathTests` 两条亦经变异验证（不注册 handler ⇒ 「幽灵在线」变红；模拟等子进程死亡 ⇒ `disconnect 耗时 2.05s` 变红）。
  · **产品侧顺带加固（保留，但不再声称它修了用户可见延迟）**：`MCPServerManager.disconnect` 顺序改为「先摘注册表与在册登记 → 再断子进程」。独立价值由变异实测给出：即使把 stop 之后人为拖 2s，注册表在返回时仍是干净的（原顺序必然残留）。
  · **当时为何误判（教训，已入 QUALITY 09-06）**：三条证据（两条不同上限的失败、低负载绿、高负载红）都更像「慢」而不是「错对象/丢更新」；纠正的钥匙不是再看日志，而是**把断言改成严格无宽限并让测试自己选对对象**——一改就红，机制随即暴露。今后凡「等待型」断言失败，先做「按身份取对象＋零宽限」两件事再谈延迟归因。
- **D-17 立项依据 2026-09-06**：见上表行（终端命令在调用方协作线程上同步等待到命令结束）。Agent 立场：推荐 (a)，因为它 5 行、语义零变更、且与本文件既有的独立线程读管道同型；(b) 是更干净的终态但改动面覆盖超时/取消/捕获上限三条语义，不宜在 MVP 收尾轮做。⚠️ 该项与既有「协作池调度停滞」环境口径**必须分开归因**，不得拿来解释那条观察。

- **D-18 立项依据 2026-09-06**：本轮为 D-16 做 `disconnect` 顺序加固时顺带审计前缀匹配规则，用两个内存客户端**当场复现**了跨服务器误删（`my` 的卸载清空了 `my_server` 的工具，见上表实测序列）。Agent 立场：推荐 (a)——它把「按前缀猜归属」换成「按已知清单精确删」，与本轮已确立的「按身份取项」是同一原则（同一轮里测试侧刚因「按下标取项」吃过亏）；(b) 是更彻底的终态但属破坏性 wire 变更，冻结期不动。

- **D-17 ✅ 2026-09-06（采 (a)，自动闭环：纯技术缺陷、修法无产品语义分歧，与你 09-06「执行」指令一致；推翻成本一条 revert）**：
  · **改法**：`withCheckedThrowingContinuation` 闭包内的阻塞体（`pollUntilExit` 50ms 轮询 + `waitUntilExit()` + `group.wait()`）整体投 `Thread.detachNewThread`；轮询节律／超时／取消／捕获上限／group 次序**逐字未动**，只换执行线程。`CheckedContinuation` 非 Sendable ⇒ 以 `ContinuationBox(@unchecked Sendable)` 完成一次性移交（全函数只有一条 resume 路径，无双续体）。
  · **改前实测前提（不是常识）**：官方称 `waitUntilExit()` 会「polls the current run loop」，而独立线程无显式 run loop ⇒ 行为不确定，必须先测。本机 A 层探针（`/tmp/probe_thread.swift`）跑同构阻塞体：**正常 20/20（含 stdout 完整捕获）／超时 10/10／取消 10/10／残留子进程 0** ⇒ 该改法可用，非脑补。
  · **判别器经一次自我否证（本项最重要的过程记录）**：第一版判据＝「并发 `cores+8` 条 `sleep 0.4` 的墙钟 < 串行下界」。开发态有效（原实现 1.045s vs 下界 0.800s 红；改后 0.534s 绿）。但**同一用例在 `main` 门禁**（Release + 覆盖率插桩 + 795 测试并行）下测得 **0.814s 压过 0.800s 阈值 ⇒ 假红**，`main` 门 rc=1。放宽阈值被否（原实现变异值 1.045s 离阈值太近，放宽即失去判别力）。⇒ 改为**线程同一性**判据：测「阻塞体跑在哪条线程」这一结构事实，与机器负载无关。
  · **测试缝的必要性（写死在此，防止后人改回墙钟）**：`runThreadProbe` 上报「阻塞体线程」与「调用方线程」两个标识。忠实变异实测（**仅去掉线程投递、probe 留在阻塞体原位**）⇒ 两标识为**同一地址**、用例红于真实机制；同一轮里并发功能条仍绿，**反证功能测试无法替代本判据**。生产路径 probe 恒为 nil，零新增等待/分配。
  · **两处 API 事实经实测定型（非推测）**：① Swift 在 async 上下文禁用 `Thread.current`（编译器报 unavailable）⇒ 取值经同步 helper `currentThreadID()`，且只做瞬时观测、不跨 await 持用；② 候选 `bitPattern(of: pthread_self())` 在本机 SDK **不存在**（`cannot find 'bitPattern' in scope`）⇒ 该路否证。
  · **本行原有一处不实引用已就地纠正**：原文称「本文件 L413/L441 已是同型用法」，实测 `grep detachNewThread` 全仓零命中，既有读者用 `DispatchQueue.global`（内核线程，本就不占协作池）⇒ 属未核实即入册（教训入 QUALITY 09-06 ⑪）。
  · 与既有「协作池调度停滞（线程空闲但任务不派发）」环境口径**仍分开归因**：本项是「线程被我们主动阻塞」，那条未复现、也不由本项解释。

- **D-18 ✅ 2026-09-06（采 (a)，实现强于原案；自动闭环理由同 D-17）**：
  · **实际判据比 (a) 更稳**：(a) 原案是「`client.listTools()` → 精确名逐个注销」，但 `disconnect` 恰恰常发生在子进程已死／握手失败时，`listTools()` 拿不到就要回落前缀 ⇒ 误删面仍在。改为**取工具注册时自带的归属**：`registry.tool(named:) as? MCPToolAdapter` → `adapter.client.name == 目标服务器`，字符串巧合与进程死活一并脱离关系。
  · **安全边界先查后改（防「误删」改成了「漏删」）**：全仓 `mcp_` 前缀生成点仅 `MCPToolAdapter.name` 一处，注册点仅 `makeTools`/`refreshTools` 两处且都产出该类型 ⇒ 严格化归属判定不会漏删；内置/Knowledge/Skill 工具一律不碰。`BuiltinTools` 的 `hasPrefix("mcp_")` 仅用于 UI 分类，不涉归属。
  · **同规则的第二处删除点一并收口**：`refreshTools(for:into:)`（`tools/list_changed` 路径）与前缀删除逻辑同源，已共用 `unregisterTools(ofServer:from:)`。
  · **判别器 3 条 + 变异实测**：回退前缀法 ⇒ 两条误删条变红（`disconnect` 得 `after=[]`，与立项实测序列逐字一致；`refreshTools` 丢 `mcp_my_server_y`）；**防漏删正向条保持绿**（它守的是「不连带删」不得退化成「干脆不删」——这正是只测误删面时最容易放过的反向缺口）。
  · **(b) 案（工具名转义）继续否掉**：属破坏性 wire 变更（工具页 id／既有会话 `isExecuted` 记录／主题与技能引用全受影响），冻结期不做。

- **D-19 立项依据 2026-09-06**：G1a 轴1 审 A17（静止态内容交叠）时顺手拉的材质清单暴露的缺口——常驻结构面 `ChatTopBar` 用 `.ultraThinMaterial` 而非 `.glassSurface(...)`，等于主题插件（本项目「主题全插件化」卖点的载体）对它零作用力：换 tint 顶栏不动、切材质顶栏不动、系统「减弱透明度」时它走 SwiftUI Material 自己的系统行为而不是我们那条已核销的降级链。Agent 立场：**不自动改**（顶栏是横跨整窗的大面，形态一改必上观感；且 tint 作用域本就挂在 D-11 待裁），但也不再默认它是「玻璃面已覆盖」，故立此卡。附带待办：Toast（`ContentView.swift:80`）同族但非结构面，可在裁决时一并豁免。

- **D-20 立项依据 2026-09-06**：并行态调查轮（QUALITY ㉙）登记的观察项 F-a 在本轮完成归因——它不是环境抖动而是**产品侧竞态**，故必须立卡而非只写在测试报告里（否则下次有人把 e2e 卸载用例当 flake 忽略，缺陷就永久隐身）。Agent 立场：**自动闭环不再等你**，因为修法只加「消失即不连接／已消失即断开」两条守卫，不改变任何用户可见语义（唯一可见差异＝卸载后不再复活，即缺陷本身）；测试缝是纯加法且默认 nil＝生产路径零介入。⚠️ 未采纳 (b)（取消在途 Task）的理由：它把竞态从「状态判定」搬到「取消传播时序」，在 SwiftUI 视图与后台 Task 混跑的启动路径上更难证明，且仍覆盖不到连接已完成的后半窗口。
