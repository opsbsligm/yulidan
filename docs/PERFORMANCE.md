# 性能基线与优化记录

## 2026-08-18 — SessionDB 启动路径 N+1 修复

**问题**：`SessionDB.loadAll()` 对每个会话行额外发起一次事件查询（N+1），且启动时全量解码所有会话的全部事件。会话数增长后启动时间与内存随之线性恶化。

**修复**（提交 8064f1a）：
- `loadSessions()`：仅元数据，单条 SQL，启动侧边栏专用
- `load(_ id:)`：单会话含事件，按需加载（切换会话 / 选中首个会话）
- App 层切换选中统一走 `presentSession`：先取事件再切换，`selectedSession` 永不指向空事件记录，防止 persist 按 messages 重建时误清空 DB

**回归测试**：`SessionDBTests.testLoadSessionsFastOnLargeDataset` — 300 会话 × 80 事件，元数据加载 < 1s。

## MemProbe 基线（debug 构建，M5 Mac，macOS 27）

命令：`swift run MemProbe 300`

每次迭代 = 1 次会话保存 + `loadSessions` + `load`（选中会话）+ 每 20 次删除最旧会话（与 App 启动/切换路径同款调用）。

| 日期 | 迭代数 | 耗时 | 单次 |
|------|--------|------|------|
| 2026-08-18 | 300 | 1.06s | ≈3.5ms |

### 修复前后对比（实测，2026-08-18，debug 构建，M5 Mac）

数据集：300 会话 × 120 事件（36,000 事件，事件含 3 个 text 块模拟真实负载）

| 路径 | 耗时 | 说明 |
|------|------|------|
| `loadAll()` 全量（旧启动路径） | 0.287s | N+1 事件查询 + 全量 JSON 解码 |
| `loadSessions()` 元数据（新启动路径） | 0.001s | 单条 SQL，无事件解码 |

**启动列表加载提速 ≈ 212×**，且不再随事件总量增长；事件解码成本转移到选中会话的按需 `load(id)`（单次约 ms 级）。

## 已知热点（未处理，留档）

- `SessionDB.save()` 每次持久化 DELETE + 全量重插 events（O(事件数)）。消息支持重试/改写历史，无法安全增量追加；会话事件量极大时再考虑分段/压缩。
- 聊天区大消息列表 SwiftUI 渲染未做虚拟化测量（当前 LazyVStack）。

后续优化项提交时在此追加基线行。

## App 层玻璃性能护栏（2026-09-03 立规，G2·静默采样口径）

背景：P1/P2 持续添加玻璃面（glassEffect）后，本文此前仅覆盖 DB 路径。v8 G2 护栏条款
「帧率/RSS 平台期/leaks 0」需先界定**哪些能静默测（A 层）**、哪些不能（如实登记，不假绿）。

### 取证通道分级（铁律 8 静默验收）

| 指标 | 静默通道 | 口径 |
|---|---|---|
| App RSS / CPU | `ps -o rss=,pcpu= -p <pid>`（不附加进程、零打扰，锁屏可跑） | 见下方平台期规则 |
| leaks | 门禁 leaks 环节（测试构建） | 四门禁在册条款，0 容忍 |
| 玻璃面数 | 静态注册表（BENCHMARK_CHECKLIST A12：容器 4 / 调用点 12 / **sessionRow 线性项**） | 运行时计数走 G3 走查（`bin/axdump` 只读快照可数窗口内元素） |
| 帧率/掉帧 | **无静默通道**（需屏幕采集或前台插桩，均违反 A 层） | ❌ 不宣称、不留空承诺 → 归 G3 人工走查项 |

### RSS 平台期规则（提案，可调）

≥10 分钟间隔采 3 次，漂移 ≤±10% 视为平台期；对同版本多轮采样建立趋势线；
新玻璃改动前后各采一组对比（G2 before/after 取证的一部分）。**单点采样不构成平台期结论**（下表如实标注）。

### 基线采样（debug 构建，M5，macOS 27 beta，App 隐藏态 windows=0）

| 时刻 | RSS | pcpu | 进程龄 | 实例口径 |
|---|---|---|---|---|
| 16:29 | 44.8 MB | 0.0% | 4h01m | 运行中旧代码（@7a853d6 时代启动）；磁盘 bundle 已被门禁重建至 sha ec9ff1ee…（@a11a4b2 源）——采样计运行实例 |
| 16:35 | 41.5 MB | 0.0% | 4h07m | 同上（采样 2） |
| 16:40 | 41.5 MB | 0.0% | 4h11m | 同上（采样 3） |

**本轮判定**：3 采样漂移 −7.4%（≤±10%），趋势平稳无泄漏征象；⚠️ 但采样间隔 6/5 分钟略低于
提案规则的 ≥10 分钟，且为隐藏态旧实例单组——**登记为「趋势平稳·平台期未正式判定」**。
下轮起对 Release 门禁构建 + 可见态各建一组正式平台期基线（G2 玻璃 before/after 取证同款方法）。

### 正式平台期基线·隐藏态组（G2 在册待办启动，09-03 18:40 起，进行中）

**载体口径（环境限制如实登记）**：可启动实例 = xcode 门禁产物 Debug bundle
（`ci-derived-data/…/Harness.app`，可执行 sha `73058939…`，@3d66ba0 源，18:28 构建）。
「Release 门禁构建」口径修正：main 环节为 SwiftPM release **无 .app 形态**，裸可执行建
NSWindow 实测 SIGTRAP（环境坑在册）不可启动 → 平台期基线以 xcode 门禁 bundle 为权威载体；
Release 侧性能结论以 leaks+编译产物一致性背书，不为 Release 单造 bundle（避免引入非门禁构建口径）。
**启动方式（静默合规实证）**：`open -g -j`（open(1) 标准旗标：不置前台+隐藏启动）——
采样时前台进程仍为原前台（System Events 只读核对）、DB 零新增（26/24 不变，启动不建会话）、
bundle 隐藏态 axdump windows=1（离屏窗，非旧表「windows=0」口径——差异如实标注）。
**口径精确化（09-03 实例反查再补正）**：SwiftPM **debug** 构建实际产出 `HarnessApp.app` bundle
（`.build/arm64-apple-macosx/debug/HarnessApp.app`，实证：该 bundle 存在一个 12:28 启动的用户侧实例
pid 58301 正常运行——非 Agent 启动、未动）；**release 目录无 bundle 实测维持**。即「裸可执行
SIGTRAP」限定于**无 bundle 的裸 exec**，SwiftPM debug bundle 可正常启动——与既有全部判定自洽。
采样器 `tools/perf/rss-sample.sh`（ps 只读）入仓。

| 点 | 时刻 | RSS | pcpu | 进程龄 | 状态 |
|---|---|---|---|---|---|
| s0 | 18:40:01 | 134.7 MB | 0.0% | 00:07 | 启动 7 秒点（含瞬态，作参照点；间隔 s0→s1=12.5min 达标，故仍计入三点） |
| s1 | 18:52:28 | 131.6 MB | 0.0% | 12:34 | vs s0 −2.3% |
| s2 | 19:03:07 | 133.8 MB | 0.0% | 23:13 | vs s1 +1.7%；vs s0 −0.7% |
| s3 | 19:35:50 | 99.8 MB | 0.3% | 55:56 | 长龄续点：启动瞬态资源释放 −25.4%（vs s2），**远低于平台期下沿——无增长趋势加固**（pcpu 0.3 为采样瞬值） |
| s4 | 19:47:03 | 100.0 MB | 0.0% | 01:07:09 | 长龄续点 2：vs s3 +0.2%——100MB 平台纹丝不动（67 分钟） |

**正式判定（隐藏态组）✅ 平台期成立**：三点全部满足 ≥10 分钟间隔（12.5/10.6 min），
最大漂移 +2.4%（≤±10% 规则），pcpu 恒 0.0%，23 分钟跨度无单调增长 → **无泄漏征象**。
口径边界如实登记：单实例单组、隐藏态（windows=1 离屏窗）、Debug bundle 载体（Release 口径限制见上）。
后续追加规则：每轮玻璃改动后对本表做 before/after 同法对比（G2 取证配套）；可见态组待用户择时。

**可见态组**：需窗口真实可见 = 改变可见窗口状态 → 按静默铁律属**择时项**（你同意时 Agent 仅
`open -g`+只读采样，或你顺手打开 App 时我后台采样亦可）。

**判定规则重申**：s1/s2 漂移 ≤±10% 才宣称平台期；RSS 含启动回落瞬态，s0 只作参照不作基线。

